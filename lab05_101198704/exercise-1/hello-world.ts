/*
var greeter = function(name) {
    console.log(' hello ' + name);
}
greeter("Rezwan Tarin");
*/
let greeter = (firstName, lastName) => {
    console.log(`hello ${firstName} ${lastName}`);
}

greeter("Rezwan","Tarin");